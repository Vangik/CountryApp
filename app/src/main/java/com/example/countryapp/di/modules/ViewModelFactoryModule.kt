package com.example.countryapp.di.modules

import com.example.countryapp.repository.impl.CountryRepositoryImpl
import com.example.countryapp.viewmodels.ViewModelFactory
import dagger.Module
import dagger.Provides

@Module
class ViewModelFactoryModule {
    //@Provides
    //fun provideChildModelView(repo: CountryRepositoryImpl) = ViewModelFactory(repo)
}